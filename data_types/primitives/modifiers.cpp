#include <iostream> 

using namespace std;


int main(){
    // int a = -1200; // signed integer as it accepts both negative and positive value
    unsigned b = -1200; // only accepts postive values and if not given can store a garbage value use e.g unsigned or unsigned int
    short c = 222;

    cout << c << endl;
    return 0;
}